<div class="container">
  <div class="row mt-3">
    <div class="col-lg-12">
      <h3 class="sub-title">ライブ登録依頼</h3>
    </div>
  </div>
  <%= form_with model: @live, url: lives_path, method: :post do |f| %>
    <%= render "admin/lives/form", f: f, live: @live %>
  <% end %>
</div>