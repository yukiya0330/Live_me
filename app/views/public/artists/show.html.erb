<div class="container artist-page">
  <div class="row mt-5">
    <div class="col-11 mx-auto d-flex justify-content-between">
      <h2 class="main-title"><%= @artist.name %></h2>
      <div id="favorite_artists_<%= @artist.id %>">
        <%= render "public/favorite_artists/btn", artist: @artist %>
      </div>
    </div>
  </div>
  <div class="row mt-1">
    <div class="col-lg-6 text-center"><%= image_tag @artist.get_artist_image(500,500), class: "img-fluid" %></div>
    <div class="col-lg-6">
      <table cellpadding="10">
        <tbody>
          <tr>
            <td><div class="mini-title">INFO</div></td>
            <td class="text-break"><%= @artist.introduction %></td>
          </tr>
          <tr>
            <td><div class="mini-title">SITE</div></td>
            <td class="text-break"><%= link_to @artist.official_url, @artist.official_url %></td>
          </tr>
          <tr>
            <td><div class="mini-title">GENRE</div></td>
            <td>
              <% @artist.genres.each do |genre| %>
                <% if customer_signed_in? %>
                  <% if genre.favorited_by?(current_customer) %>
                    <%= link_to genre.name, genre_favorite_genres_path(genre), method: :delete, class: "default-btn bg-warning p-2 w-auto text-dark", data: {confirm: "現在お気に入りに登録されています 登録を解除しますか？"} %>
                  <% else %>
                    <%= link_to genre.name, genre_favorite_genres_path(genre), method: :post, class: "default-btn bg-warning p-2 w-auto text-dark", data: {confirm: "登録しますか？"} %>
                  <% end %>
                <% else %>
                    <%= link_to genre.name, "#", class: "default-btn bg-warning p-2 w-auto text-dark" %>
                <% end %>
              <% end %>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
  <div class="row mt-3">
    <div class="col-lg-10 mx-auto">
      <h4 class="sub-title">このバンドの良い所!!</h4>
    </div>
    <div class="col-lg-10 mx-auto">
      <% if customer_signed_in? %>
        <%= render "public/comments/form", comment: @comment, artist: @artist %>
      <% else %>
        <div class="text-center my-5">
          会員登録を行うとコメントできます!!
        </div>
      <% end %>
    </div>
  </div>
  <div class="row mt-3 comment-list">
    <div class="col-lg-10 mx-auto">
      <h4 class="sub-title">コメント一覧</h4>
    </div>
    <div class="col-lg-10 mx-auto comments_area">
      <%= render "public/comments/index", artist: @artist %>
    </div>  
  </div>
  <div class="row my-3">
    <div class="col-lg-10 mx-auto">
      <h4 class="sub-title">ライブ予定</h4>
    </div>
    <div class="col-lg-10 mx-auto">
      <% if @artist.lives.where("live_date >= ?", Date.today).present? %>
        <table class="table table-borderless shadow p-3 bg-body rounded table-responsive">
          <tbody>
            <% @artist.lives.where("live_date >= ?", Date.today).where(status: 1).each do |live| %>
              <tr>
                <td style="width: 10%" class="align-middle text-center"><%= image_tag live.get_live_image(50,50) %></td>
                <td style="width: 20%" class="align-middle"><%= link_to live.name, live_path(live) %></td>
                <td style="width: 25%" class="align-middle">
                  <% live.performing_artists.first(3).each do |performing_artist| %>
                    <%= performing_artist.artist.name %>
                  <% end %>
                </td>
                <td style="width: 25%" class="align-middle">
                  <% live.live_schedules.each do |live_schedule| %>
                    <%= live_schedule.live_house.name %>
                  <% end %>
                </td>
                <td style="width: 15%" class="align-middle">START: <%= live.start_time.strftime( "%H:%M" ) %></td>
                <td style="width: 5%" class="align-middle">
                  <% if customer_signed_in? %>
                    <div id="bookmark_lives_<%= live.id %>">
                      <%= render "public/bookmark_lives/btn", live: live %>
                    </div>
                  <% end %>  
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        <p class="my-5 text-center">
          現在、ライブの予定はありません</br>
          ライブ日程の追加がある場合は、<%= link_to "ここ", new_live_path %>をクリックしてください
        </p>
      <% end %>
    </div>
  </div>
</div>