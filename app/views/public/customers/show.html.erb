<div class="container">
  <div class="row mt-3">
    <div class="col-3 text-center">
      <div class="py-3 bg-light rounded-lg">
      <%= image_tag current_customer.get_profile_image(200,200), class: "rounded-circle" %><br>
      <%= current_customer.nickname %><br>
      <%= link_to "編集", edit_customer_path(current_customer), class: "btn btn-success" %>
      </div>
    </div>
    <div class="col-9">
      <div class="row mt-3">
        <div class="col-12">
          <h4 class="col-8 sub-title">まだ見ぬアーティストを発掘！</h4>
          <div class="my-5 text-center">ここに挿入</div>
        </div>
      </div>  
      <div class="row mt-3 bookmark-list">
        <div class="col-12">
          <h4 class="col-7 sub-title">ブックマークしたライブ</h4>
          <% if current_customer.bookmark_lives.present? %>
            <table>
              <tbody>
                <% current_customer.bookmark_lives.each do |bookmark_live| %>
                  <tr class="d-flex align-items-center">
                    <td style="width: 10%"><%= image_tag bookmark_live.live.get_live_image(50,50) %></td>
                    <td style="width: 60%"><%= link_to bookmark_live.live.name, live_path(bookmark_live.live) %></td>
                    <td style="width: 20%">START: <%= bookmark_live.live.open_time.strftime( "%H:%M" ) %></td>
                    <td style="width: 10%">
                      <%= link_to live_bookmark_lives_path(bookmark_live.live), method: :delete, data: {confirm: "削除しますか？"}  do %>
                        <i class="fas fa-solid fa-bookmark text-success"></i>
                      <% end %>  
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          <% else %>
            <p class="my-5 text-center">ブックマークしたライブがありません</br>気になるライブをブックマークしてみましょう！</p>
          <% end %>
        </div>
      </div>
      <div class="row mt-3 favorite-list">
        <div class="col-7">
          <h4 class="col-10 sub-title">お気に入りのアーティスト</h4>
          <% if current_customer.favorite_artists.present? %>
            <table style="width: 100%">
              <tbody>
                <% current_customer.favorite_artists.each do |favorite_artist| %>
                <tr class="d-flex align-items-center">
                  <td style="width: 20%"><%= image_tag favorite_artist.artist.get_artist_image(50,50) %></td>
                  <td style="width: 70%"><%= link_to favorite_artist.artist.name, artist_path(favorite_artist.artist) %></td>
                  <td style="width: 10%">
                    <% if favorite_artist.artist.favorited_by?(current_customer) %>
                      <%= link_to artist_favorite_artists_path(favorite_artist.artist), method: :delete do %>
                        <i class="fas fa-heart" aria-hidden="true" style="color: red;"></i>
                      <% end %>
                    <% else %>
                      <%= link_to artist_favorite_artists_path(favorite_artist.artist), method: :post do %>
                        <i class="fas fa-heart" aria-hidden="true"></i>
                      <% end %>
                    <% end %>
                  </td>
                </tr>
                <% end %>
              </tbody>
            </table>
          <% else %>
            <p class="my-5 text-center">お気に入りのアーティストがいません</br>好きなアーティストをお気に入りにしてみましょう！</p>
          <% end %>
        </div>
        <div class="col-5 favorite-list">
          <h4 class="col-12 sub-title">お気に入りのジャンル</h4>
          <% if current_customer.favorite_genres.present? %>
            <table>
              <tbody>
                <% current_customer.favorite_genres.each do |favorite_genre| %>
                <tr class="d-flex align-items-center">
                  <td style="width: 100%">
                    <%= link_to favorite_genre.genre.name, genre_favorite_genres_path(favorite_genre.genre), method: :delete, class: "btn btn-warning", data: {confirm: "削除しますか？"} %>
                  </td>
                </tr>
                <% end %>
              </tbody>
            </table>
          <% else %>
            <p class="my-5 text-center">お気に入りのジャンルがありません</br>好きなジャンルをお気に入りにしてみましょう！</p>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>