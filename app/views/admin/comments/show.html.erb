<div class="container">
  <div class="row mt-5">
    <h3 class="main-title"><%= @comment.artist.name %></h3>
  </div>
  <div class="row mt-3">
    <%= @comment.comment %>
  </div>
  <div class="row mt-1">
    <div class="col-12 text-center">
    <%= link_to "削除", admin_comment_path(@comment), method: :delete, class: "btn default-btn bg-danger p-2", data: {confirm: "削除しますか？"} %>
    </div>
  </div>
</div>