<div class="container">
  <div class="row mt-5">
    <%= @artist.name %>のジャンルは・・・？
  </div>
  <div class="row mt-3">
    <%= form_with model: ArtistGenre.new, url: admin_artist_artist_genres_path, method: :post do |f| %>
      <%= f.collection_select :genre_id, Genre.all, :id, :name, include_blank: "選択して下さい" %>
      <%= f.hidden_field :artist_id, value: @artist.id %>
      <%= f.submit "登録" %>
    <% end %>
  </div>
</div>