<%= render 'layouts/flash_messages' %>
<div class="container">
  <div class="row mt-3">
    <div class="col-lg-12">
      <h3 class="sub-title">ライブ新規登録</h3>
    </div>
  </div>
  <%= form_with model: @live, url: admin_lives_path, method: :post do |f| %>
    <%= render "admin/lives/form", f: f, live: @live %>
  <% end %>
</div>